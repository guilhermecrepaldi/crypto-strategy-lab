# B10 independent preflight review

Review date: 2026-09-07. Reviewer: independent GPT-6 Astra agent. Decision: **PASS_CONDITIONAL** for preregistered `CONDITIONAL_PILOT` A/B/C execution-envelope runs only. This is not acceptance of economic results or certification of historical Binance execution. D remains NOT_CALIBRATED / INCONCLUSIVE_EXECUTION_DATA.

Numeric-freeze addendum: conditional scope now also includes `B_FEE10_PROMOTION_ABSENT`, identical to B except maker/taker rates of 0.001 each (10bps per side). This is the official regular-tier promotion-absent counterfactual, not a claim about actual historical pair fees. Four runnable profiles are reviewed; D is explicitly not runnable. A/B/C signal-to-activation proxies are respectively 1,174,720 / 1,179,525 / 1,194,469 microseconds; queue proxies are 2,109,490 / 2,330,544 / 5,509,195 USDC; all use the positive 299,570-microsecond replenishment cadence. Joint-state bid depth is retained separately from maximum bid/ask queue proxy. The narrow sampled RTT range is reported honestly: p99 of 12 is the observed sample maximum, not a certified adverse tail. Zero extra slippage is conditional best-depth execution; future adverse trade-side prices still change release execution.

The pre-authorization numeric draft SHA256 is `b10dedd4943fa55d5a4580c8941bcc232d1eb1f3b9dcae4bc2ceac2a0879f226` (`execution_authorized=false` when checked). Independently verified all four referenced file hashes: history `553550be53b657c9523bda59d905b1b987f7f86310db838ca0e47dd625a4359d`; fresh archive audit `8eefb7d66c2bf7a8cfe4676c65302df28396800a58970a5404e9c266033b27c7`; calibration `04fd09fc048602273276ecdebe583438e3dea3d28f9be6a0e7d997313d9019d6`; official rules `c4f2f9fdb1a8876c8cc869cd12963a1433d66eaf90df9fc11a7c3e299c74ffee`. Changing the authorization flag produces a distinct published config hash which the actual run must bind.

Scope inspected: `src/crypto_strategy_lab/microstructure/b10_reality.py`, `scripts/run_b10_reality.py`, `scripts/freeze_b10_execution_profiles.py`, the original `RecoveryReserveRuntime` and serial selection/reselection authorities, collector production path and fixtures. No private endpoints or real orders are present in these execution paths.

Previously blocking implementation defects were corrected in the reviewed code: remaining basis now decreases with partial sales; executed dust keeps separate unrealized basis; reserve cannot fund unsold dust or mint capital; a normal HIGH fill winning cancellation remains an ordinary cycle; hourly release scheduling no longer stalls while a release is in progress; post-release destination/tick and post-ordinary-exit reselection are applied; exact canonical event ordinal is carried from raw/cache alignment into ordinary-exit selection. Strict later-than-activation execution remains separate from canonical strategy prefix ordering.

The runner binds frozen B10 artifact identity, M007 selector hash, tape hash, January-to-physical-cutoff interval, manifest hash and published config. Raw timestamps/prices align event-by-event with the canonical tape; complete replay checks expected count and final timestamp. The profile builder checks fresh physical archive audit and calibration artifact hashes before freezing. Preserve these evidence files; changing them invalidates their dependent freeze. Queue, fills, commissions, partial settlement and reserve are calculated by code; no strategy PnL is consulted by the builder.

Conditional limitations are material: current quantity/notional filters are historical proxies; dynamic reference-price/execution-range history is unknown; the accepted-order throttle is not the exact account ORDERS-counter implementation; zero-fee eligibility across the full interval is unproven; private latency is parameterized from public transport; trade-side BBO is inferred; cadence-based depth refresh is a replenishment assumption. Selecting one observed joint state at each queue percentile retains its spread/depth coordinates but holds that proxy across historical regimes. C is an upper observed queue-difficulty state, not evidence of adverse conditions on every execution dimension. None of these results may be promoted to a historical executable-profit claim.

The profile description must match the implemented trade-side BBO alignment, not an obsolete midpoint description. D must not be numerically fabricated from a stable pilot. The official promotion-absent fee sensitivity remains a separately labeled required follow-up, not completed by the zero-fee A/B/C runs. A short pilot is allowed for conditional falsification; no arbitrary 24-hour OWNER prerequisite is imposed.

Validation evidence: collector actual `collect()` integration has 9 passing tests, supplementing 13 collector/public-client fixtures. These cover real receiver/queue/statistics/persistence with only public transport mocked: epoch microseconds, raw trade validation, zero-data incomplete status, nonoverlapping residence, hash persistence and failures. Execution agent reports 19 execution/bridge fixtures passing, including partial basis, cancel race and canonical ordinal parity; this review inspected their covered paths and does not substitute that report for an independent economic audit.

Before result acceptance, independently reconstruct at least 100 ordinary completed cycles (all if fewer) and every release attempt, plus the complete financial ledger, using raw trade support and immutable profile assumptions. Failed/partial IOC attempts need explicit expiration/activation evidence, requested from the executor; absent instrumentation is an audit limitation, not permission to infer fills. Residual inventory, dust, uncovered deficits and every unknown must remain visible. Economic results have not been reviewed or accepted by this preflight.

Reporting clarification: theoretical B10 loss divides by the operating bank (cash plus remaining basis); actual lot loss divides by lot cost. With fixed-100 sizing and accumulated idle profits these differ. Preserve the trigger and report both actual denominators, including the frozen signal-bank amount. The independent audit script now supports streamed JSONL, reconstructs every financial fill/settlement without engine helpers, verifies sampled raw aggressor/quantity/price and cumulative queue support, and audits all release signal cycles/attempts with conditional book anchors. Five synthetic integration/corruption tests pass; real-run audit remains pending.

Final timer/journal delta inspected: clock callbacks now refresh dated execution rules before pending activation or new submission; already active orders remain grandfathered. Completed-checkpoint resume verifies the durable journal prefix before regenerating missing result artifacts. The new clock-transition fixture covers activation without an intervening trade. No remaining blocking money, frozen-strategy or causal defect was identified within this conditional scope. This conclusion does not remove the explicitly unknown historical execution rules or the mandatory post-run independent audit.
