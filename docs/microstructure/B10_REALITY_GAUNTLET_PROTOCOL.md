# B10 Binance Reality Gauntlet protocol

Status: **execution protocol; numeric profiles pending calibration; not an economic result**. The OWNER's B10 directive supersedes conflicting M011 continuation. Only `B10_FROZEN` is tested under `BINANCE_REALITY_V1`. No M011, strategy optimization, second pair, second lot, private API, Testnet or live order is authorized. Official rule evidence is [Binance execution authority](../binance/BINANCE_USDCUSDT_EXECUTION_AUTHORITY.md).

## Freeze and provenance

Reconcile `RRV2_H1_B10_F0` under artifact identity `097abdab8225038b091cb721bdd36bedb8a636c94bed1c87120adc0c9e10953a`. Bind original config, implementation provenance, summaries, checkpoints, raw tape manifests, exact cutoff and independent arithmetic. Expected reference is 3,580,880 cycles, 3 zero days, 245/248 active days, 156 releases, 26h max hold, 4h lock over 24h and approximately 0.16636% maximum drawdown; these are assertions to verify physically, never replacement evidence. Historical compounding is an appendix only.

Freeze selector ancestry, LOW/HIGH, H1, theoretical B10 loss condition, original/destination activity, full theoretical reserve coverage, 2% skim, floor zero, post-release FLAT then new LOW. B10 receives the causal price prefix and actual serial-position state. Delayed fills can change subsequent opportunities; do not force its historical decision schedule onto a different inventory ledger. Simulator changes get an execution-profile identity; strategy changes require a new Mn outside this campaign.

## Capital, sizing and honest inventory

Primary ruler is fixed notional 100 USDT with initial operating cash 100 and reserve 5. Each entry spends at most `min(100, available operating cash)` after applicable costs and quantity rounding. Profits remain accounted for but cannot compound order size above 100. Losses may shrink future orders; never recapitalize from nowhere. Use Decimal/fixed-point arithmetic, segregated cash/inventory/reserve/dust and explicit per-fill cost basis.

BUY commission in base reduces sellable quantity. Round sell quantity down to valid step and retain base dust as a separate asset. A fully executed sell order is not proof the entire economic lot was disposed: report residual dust and its marked value separately. Completion requires the intended buy and sell orders fully filled, with nontradable dust transparently retained and excluded from next-lot sizing. Tradable residual inventory keeps the lot occupied. No dust conversion or disposal is fabricated. If this definition affects comparison, publish both order-complete and inventory-clear counts.

For a completed cycle, net realized profit is actual sale proceeds minus corresponding acquisition basis and all fees/costs. Fund reserve with exactly `0.02 * max(net realized cycle profit, 0)`. Do not charge spread/slippage twice: actual fill prices already incorporate price impact; cost breakdowns are explanatory decompositions relative to a frozen benchmark. Fees are actual ledger deductions. Sum cash + conservatively marked inventory + dust + reserve and reconcile changes to external market PnL/costs only.

## Causality and execution contract

Minimum states: FLAT, BUY_SUBMITTING, BUY_WORKING, BUY_PARTIAL, LONG, SELL_SUBMITTING, SELL_WORKING, SELL_PARTIAL, CYCLE_COMPLETE, RELEASE_SUBMITTING, RELEASE_PARTIAL, RELEASE_COMPLETE, CANCEL_PENDING, plus explicit rejection/gap/terminal-deficit states. Persist quantities and pending callbacks; restarting must reproduce the event/ledger hash.

Signal time, market-data receipt, decision completion, outbound arrival, exchange activation and ACK are distinct. No fill at or before activation. Canonical same-timestamp tape ordering may update the strategy prefix, but cannot justify a new order filling retroactively within that timestamp. No later HIGH, volume, book or release outcome enters an earlier decision. Each trade's quantity can be allocated at most once across queue and our order; a completed BUY never gives its supporting trade to a later SELL.

Normal orders use explicit `LIMIT_MAKER` at frozen LOW/HIGH. A modeled crossing rejects; no automatic taker conversion. With unavailable historical BBO this decision is part of the calibrated envelope, not an observed exchange rejection. Current filter values can be a labeled proxy scenario, never silently historical. Reject off-grid prices without rounding LOW/HIGH. Historical filter unknowns and rollout windows remain visible in every verdict.

Compatible exact-level aggressor volume after activation first consumes parameterized queue ahead, then at most remaining order quantity. Price touch, wrong-side volume and earlier trades contribute zero. Better-price trade-through is not automatically a full fill; any such treatment requires a separately preregistered liquidity assumption. Queue cannot be known from trade prints; no unmeasured cancellation credit or zero-queue primary case. Trade participation and hidden/replenishing liquidity assumptions must be frozen before replay. Partial BUY occupies the lot and does not start a full SELL. Partial SELL does not complete the cycle.

Cancel request leaves the order exposed until its modeled exchange cancellation boundary. Apply racing fills, reconcile remaining inventory, then allow replacement after confirmed terminal state. Do not issue two incompatible orders or resell filled quantity. Rate limits apply to every modeled message under dated/parameterized rules; successful fills may reduce unfilled-order count as documented. A data gap freezes new actions, invalidates book-dependent fills and triggers resynchronization. A gap never means no market movement or guaranteed cancel success.

## Release: retain B10 trigger, execute its consequences

The frozen theoretical release predicate is unchanged. Select a supported market-like sale or marketable IOC limit execution contract before replay; no new economic stop threshold is allowed. Its price protection, partial-expiry behavior, cancel-first race and retries are execution mechanics and require explicit frozen rules. IOC remainder is still inventory, never FLAT. New submissions incur latency and message accounting.

Use bid-side capacity at arrival from observed historical L2 if available, otherwise calibrated trade-flow/depth envelopes. Do not equate the next public SELL print with our aggressive sale. Log theoretical signal price/deficit separately from actual fill VWAP, fees and shortfall. Actual loss may exceed 10bp and is never clamped. If evidence cannot support liquidation quantity, leave it unfilled.

Preserve the frozen loss-cap denominator: the original predicate divides deficit by operating cash plus remaining inventory basis. Under fixed notional, retained idle profits mean this operating bank can exceed the executed lot. Report `THEORETICAL_LOSS_BPS_OPERATING_BANK`, `ACTUAL_LOSS_BPS_EXECUTED_LOT` and `ACTUAL_LOSS_BPS_SIGNAL_BANK` separately; the last uses the exact operating-bank amount captured at the signal. Separate their counts above 10bps. Never silently change the predicate denominator to 100 or compare unlike denominators as if execution alone caused the difference.

Adopt atomic settlement after complete release for reserve accounting: accumulate executed fills and realized allocated deficit in a pending ledger; transfer only after release completion and only up to available reserve. Record any uncovered executed deficit and leave operating capital impaired; reserve cannot become negative. Partial release holds inventory and pending loss; report marked equity continuously. Exhaustion does not allow a magical restored bank or a second lot. Record blocked future releases under the original coverage predicate using actual reserve. No replenishment, additional cap/floor or emergency exit is invented.

## Calibration and immutable profile gate

Collect only public USDCUSDT trades, BBO, depth diffs and periodic snapshots. Preserve raw timestamps/units, receive UTC and monotonic time, sequence IDs, snapshot IDs, gaps, requests and hashes. Measure REST RTT, event-to-receive distributions with clock-skew caveats, reconnects, spread, best/one-tick depth, trade intensity, BBO residence and churn. bookTicker without event time cannot produce exchange-to-receive latency.

Calibration must predeclare acquisition duration, minimum valid synchronized coverage, sampling weights, gap rejection and partitioning before choosing numerical quantiles. A short quiet sample is not evidence of peg stress or a P99 tail. Use time-weighted book states to avoid overcounting high-churn states. Report sample counts and confidence/representativeness limits. Public RTT is not private order ACK; outbound/engine/ACK values remain parameterized and nonzero.

Freeze four profiles only after calibration, before economic results: A OBSERVED_BEST_SUPPORTED; B REALISTIC_CONSERVATIVE; C ADVERSARIAL_PLAUSIBLE; D PEG_STRESS. Each binds calibration hashes, rule timeline, fee eligibility, queue mapping, arrival/cancel/ACK distributions, sampling seed, depth/participation, spread, slippage, release behavior, gap policy and historical proxy flags. Preserve correlated joint conditions where supported; do not combine unrelated marginal extremes into an impossible market. Unsupported peg-stress numbers keep D pending. Fee sensitivity crosses execution conditions independently: zero-fee evidence must not be silently replaced by standard fees in a purported realistic profile. Exact account rates remain unknown.

No numeric queue, latency or slippage value is selected by this document. Missing historical L2 permits envelopes; it does not permit arbitrary uncalibrated numbers. Missing calibration blocks **numeric profile freeze/full replay acceptance**, while collector, simulator fixtures and manifests continue. The run gate additionally requires published source SHA, immutable input hashes, complete tape audit and reviewed strategy-to-execution wiring.

A valid short pilot may support explicitly conditional full-span execution envelopes once its measurements and parameter mappings are frozen. This is not certification that the sample represents historical execution. There is no OWNER-mandated 24-hour collection gate. For such pilot envelopes preserve synchronized joint market blocks and time weights; choose actual blocks with a preregistered observable difficulty ranking rather than combining unrelated marginal extremes. Label profiles `CONDITIONAL_PILOT`, separate observed inputs from private-latency/price-alignment assumptions, and retain `INCONCLUSIVE_EXECUTION_DATA` where extrapolation prevents a real-world verdict. A worst observed stable block is not automatically peg stress; D remains unqualified when no stress evidence exists. No conditional replay may be presented as an accepted fully calibrated gauntlet.

## Validation, audit and full interval

Required fixtures: touch without fill; wrong aggressor; latency miss; partial buy/sell; cancel race; queue depletion; full fill; quantity/dust rounding; min-notional rejection; fee erasing edge; release above 10bp; partial release; reserve depletion; net-profit funding; book gap/resnapshot; same-timestamp causality; restart determinism. Add fee-base inventory, dynamic-rule unknown, rate-limit and cash-conservation checks where implementation needs them.

Before accepting results independently reconstruct at least 100 ordinary completed cycles (or all if fewer) and every release, including partial/failed attempts. Freeze deterministic sample selection and inspect timestamps, supporting trade IDs/quantities/aggressor, fees, inventory and reserve. Audit code must not merely call the simulator's own accounting helpers. Preserve rejected/zero-fill attempts for causal explanations.

Every frozen profile traverses the complete canonical development interval; economic failure is not early-stop authority. Technical invalidity preserves checkpoints and is explicitly classified. End-of-tape open positions remain open/marked, with no synthetic final sale. No profile is tuned after observing economics. Independent review precedes acceptance; inconclusive data is a legitimate result.

## Scoreboard and decisions

Reference denominator remains 3,580,880. Report full-fill and net-positive counts separately, their respective retention ratios, and ordinary opportunity funnel: reference opportunity, level trade, compatible flow, active order, queue cleared, partial/full BUY, active SELL, cleared SELL queue, partial/full SELL, gross completion, positive net completion. Because execution changes the path, distinguish historical opportunity mapping from new endogenous orders; never claim a simple survivor subset without matching IDs.

For every profile include all OWNER fields: full/positive cycles, zero/active days, daily average/median, buy/sell full-fill rates, partial rate, misses by latency/queue/filter/fee, gross/net fixed-100 PnL, fees/slippage, per-cycle edge, release signals/full/partial, theoretical/executed loss, executed releases above 10bp, reserve initial/funding/consumption/min/final/depletion, max hold, lock over 24h, drawdown and verdict. Also report reserve coverage ratio, reserve-blocked releases, rejected orders, message load, residual dust/inventory, gaps and unknown-rule exposure. Use explicit denominators, UTC calendar days, non-overlapping miss attribution and null for not measured.

Verdicts are OWNER classes: DESTROYED; SEVERELY_DEGRADED_BUT_POSITIVE; SURVIVES_REALISTIC_CONSERVATIVE; SURVIVES_ADVERSARIAL_PLAUSIBLE; INCONCLUSIVE_EXECUTION_DATA. Nonpositive supported net edge or failure of the mechanism supports destruction; a deliberately counterfactual fee stress alone does not prove actual historical destruction. Positive net edge alone does not establish survival: reserve coverage, continuity, residual inventory and unresolved execution assumptions must also support it. Quantitative acceptance thresholds must be frozen before results; until then, no automatic survival verdict.

Artifacts: rule/data/calibration/profile manifests; rule timeline; B10 exact reconciliation; per-profile scoreboard; release autopsy; independent audit and final report. This protocol creates no economic outputs and claims no completed replay.
