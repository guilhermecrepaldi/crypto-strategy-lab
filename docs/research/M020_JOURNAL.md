# M020 research journal — zonal ping-pong 5h

Append-only journal for `STABLECOIN_ZONAL_PING_PONG_5H_V1`.

## 2026-09-08 — OWNER authority received

- New model required; M019 remains preserved with 10 D1 cycles and its
  `INVENTORY_LOCK` autopsy.
- Authorized economic interval is exactly the first five UTC hours of
  2025-01-01. No second day or safe-min-notional replay is authorized.
- The one-USDT-equivalent unit is a non-executable structural diagnostic below
  Binance minimum notional.
- Full-calendar-2025 canonical trades are authorized for descriptive P80/P90/
  P95/P99 geometry. This makes D1 DEVELOPMENT, never out-of-sample.

## 2026-09-08 — occupancy geometry completed

- 365 validated archives and 166,291,613 canonical trades processed.
- Unknown prefix: 6,766 microseconds; known coverage fraction:
  0.9999999997854515474378488077.
- Annual observed low/high: 0.98500 / 1.00900.
- P80: 0.99940–1.00030, retaining 80.08038628823642483594012083% of
  known time.
- With source-day tick 0.0001, P80 yields 10 distinct endpoints and nine
  immutable physical bands. No duplicate bands were manufactured to reach 40.
- Economic replay has not started. Implementation, tests, review, registration
  and publication remain pending.

## 2026-09-08 — implementation and independent preflight

- Implemented immutable nine-band geometry, rolling four-by-four free-entry
  selection, persistent owned exits, per-band sequence identity, global trade
  budget, observed queue and exact five-hour cutoff.
- The first independent GPT-6 Astra review blocked execution. Successive
  adversarial reviews exposed activation-time post-only errors, cancel-pending
  lifecycle, fragmented-fill identity, reverse-cycle recycling, unsegregated
  reentry proceeds, incomplete queue/financial audit, free-quote classification,
  cross-band basis drift and JSON checkpoint typing. No economic result was
  generated while any blocker remained.
- Each reproduced defect received a regression test. Inventory now uses exact
  cost layers rather than rounded average basis as authority; independent audit
  reconstructs canonical prints, queue, both cycle legs, liquidity and finances.
- Final focused validation: 34 tests passed. Five additional deterministic
  adversarial sequences of 200 book/trade pairs passed kernel invariants and the
  independent audit. `TEST_SUITE_PASS != STRATEGY_PASS`.
- Independent preflight verdict: `PASS_CONDITIONAL_PRE_RUN`, reviewer model
  `gpt-6-astra`. The normalized result remains below Binance minimum notional,
  DEVELOPMENT-only and non-live-executable.
- Economic replay has not started. Registry creation and publication remain the
  next gates.

## 2026-09-08 — M020 registered; pre-run publication pending

- Registry identity: `M020`, status `CREATED`, model hash
  `d170be7d81e6a93a2bc04ce74b50775f9d054dd2f2fbf2d674f0afa2b5bab45b`.
- The machine gate authorizes only M020 and only the frozen five-hour interval.
  Day2, safe-min-notional execution, parameter sweep, Testnet and live remain
  closed.
- `TEST_SUITE_PASS != STRATEGY_PASS`; no economic replay has started. The next
  required action is commit/push of the exact preregistered source and evidence.
- Final repository validation before publication: 724 passed, 1 skipped and 1
  integration test deselected. Ruff and focused strict mypy passed. These are
  software results only; `STRATEGY_VERDICT=PENDING`.
